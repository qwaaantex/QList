name: qlist
description: "A simple notes"

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.7.0

dependencies:

  cupertino_icons: ^1.0.8
  provider: ^6.1.2
  searchfield: ^1.2.6
  flutter:
    sdk: flutter

dev_dependencies:

  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter
 

flutter:

  uses-material-design: true
